spring:
  application:
    name: order-service
  datasource:
    url: jdbc:h2:mem:order_service
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
management:
  endpoints:
    web:
      exposure:
        include: health,info
