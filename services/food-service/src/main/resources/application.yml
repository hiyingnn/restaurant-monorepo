spring:
  application:
    name: food-service
  datasource:
    url: jdbc:h2:mem:food_service
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
management:
  endpoints:
    web:
      exposure:
        include: health,info
      show-details: always
